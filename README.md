# YesNo React Application

To learn Jest testing in basic React app

## Setup

This project is a `create-react-app` project. To set it up, just execute following commands:

```
npm install
nm start
```

And to run tests:

```
npm test
```